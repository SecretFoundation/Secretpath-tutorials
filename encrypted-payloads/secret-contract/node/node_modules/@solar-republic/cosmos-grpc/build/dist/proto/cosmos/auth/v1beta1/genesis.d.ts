import type { Encoded, JsonAny, Opt } from '../../../../api/types.js';
import type { CosmosAuthParams, DecodedCosmosAuthParams } from './auth.js';
import type { DecodedGoogleProtobufAny } from '../../../google/protobuf/any.js';
export type EncodedCosmosAuthGenesisState = Encoded<'/cosmos.auth.v1beta1.GenesisState'>;
/**
 * Encodes a `GenesisState` protobuf message: defines the auth module's genesis state.
 * @param g_params - `params`: defines all the parameters of the module.
 * @param a_accounts - `accounts`: the accounts present at genesis.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeCosmosAuthGenesisState: (atu8_params?: Opt<Encoded<'/cosmos.auth.v1beta1.Params'>>, a_accounts?: Opt<Uint8Array[]>) => EncodedCosmosAuthGenesisState;
/**
 * A decoded protobuf GenesisState message
 *
 * Tuple where:
 *   - 0: params - defines all the parameters of the module.
 *   - 1: accounts - the accounts present at genesis.
 */
export type DecodedCosmosAuthGenesisState = [
    a_params?: DecodedCosmosAuthParams,
    a_accounts?: DecodedGoogleProtobufAny[]
];
/**
 * Decodes a protobuf GenesisState message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedCosmosAuthGenesisState}
 */
export declare const decodeCosmosAuthGenesisState: (atu8_payload: Uint8Array) => DecodedCosmosAuthGenesisState;
/**
 * Destructures the fields of a {@link CosmosAuthGenesisState} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: params - defines all the parameters of the module.
 *   - 1: accounts - the accounts present at genesis.
 */
export declare const destructCosmosAuthGenesisState: (g_struct: CosmosAuthGenesisState) => [params?: CosmosAuthParams, accounts?: JsonAny[]];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_COSMOS_AUTH_GENESIS_STATE = "/cosmos.auth.v1beta1.GenesisState";
/**
 * JSON serialization of `cosmos.auth.v1beta1.GenesisState` - defines the auth module's genesis state.
 */
export type CosmosAuthGenesisState = {
    params?: CosmosAuthParams | undefined;
    accounts?: JsonAny[] | undefined;
};
