import { __UNDEFINED } from '@blake.regalia/belt';
import { decode_protobuf, decode_coin } from '../../../../api/protobuf-reader.js';
import { Protobuf, coin } from '../../../../api/protobuf-writer.js';
/**
 * Encodes a `GenesisState` protobuf message: defines the crisis module's genesis state.
 * @param a_constant_fee - `constant_fee`: the fee used to verify the invariant in the crisis module.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export const encodeCosmosCrisisGenesisState = (a_constant_fee) => Protobuf() // ...
    .b(coin(a_constant_fee), 3) // Coin constant_fee = 3
    .o;
/**
 * Decodes a protobuf GenesisState message
 * @param atu8_payload - raw bytes to decode
 * @param RESERVED - a second argument is explicitly forbidden. make sure not to pass this function by reference to some callback argument
 * @returns a {@link DecodedCosmosCrisisGenesisState}
 */
export const decodeCosmosCrisisGenesisState = ((atu8_payload, [, , a_constant_fee] = decode_protobuf(atu8_payload, [0, 0, 1], [0, 0, decode_coin])) => [__UNDEFINED, __UNDEFINED, a_constant_fee]);
/**
 * Destructures the fields of a {@link CosmosCrisisGenesisState} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 2: constant_fee - the fee used to verify the invariant in the crisis module.
 */
export const destructCosmosCrisisGenesisState = (g_struct) => [__UNDEFINED, __UNDEFINED, g_struct.constant_fee];
/**
 * Message type string constant
 */
export const SI_MESSAGE_TYPE_COSMOS_CRISIS_GENESIS_STATE = '/cosmos.crisis.v1beta1.GenesisState';
//# sourceMappingURL=genesis.js.map