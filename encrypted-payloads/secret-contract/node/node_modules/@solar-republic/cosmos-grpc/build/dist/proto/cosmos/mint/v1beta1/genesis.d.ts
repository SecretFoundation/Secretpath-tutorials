import type { Encoded, Opt } from '../../../../api/types.js';
import type { CosmosMintMinter, DecodedCosmosMintMinter, CosmosMintParams, DecodedCosmosMintParams } from './mint.js';
export type EncodedCosmosMintGenesisState = Encoded<'/cosmos.mint.v1beta1.GenesisState'>;
/**
 * Encodes a `GenesisState` protobuf message: defines the mint module's genesis state.
 * @param g_minter - `minter`: a space for holding current inflation information.
 * @param g_params - `params`: defines all the parameters of the module.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeCosmosMintGenesisState: (atu8_minter?: Opt<Encoded<'/cosmos.mint.v1beta1.Minter'>>, atu8_params?: Opt<Encoded<'/cosmos.mint.v1beta1.Params'>>) => EncodedCosmosMintGenesisState;
/**
 * A decoded protobuf GenesisState message
 *
 * Tuple where:
 *   - 0: minter - a space for holding current inflation information.
 *   - 1: params - defines all the parameters of the module.
 */
export type DecodedCosmosMintGenesisState = [
    a_minter?: DecodedCosmosMintMinter,
    a_params?: DecodedCosmosMintParams
];
/**
 * Decodes a protobuf GenesisState message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedCosmosMintGenesisState}
 */
export declare const decodeCosmosMintGenesisState: (atu8_payload: Uint8Array) => DecodedCosmosMintGenesisState;
/**
 * Destructures the fields of a {@link CosmosMintGenesisState} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: minter - a space for holding current inflation information.
 *   - 1: params - defines all the parameters of the module.
 */
export declare const destructCosmosMintGenesisState: (g_struct: CosmosMintGenesisState) => [minter?: CosmosMintMinter, params?: CosmosMintParams];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_COSMOS_MINT_GENESIS_STATE = "/cosmos.mint.v1beta1.GenesisState";
/**
 * JSON serialization of `cosmos.mint.v1beta1.GenesisState` - defines the mint module's genesis state.
 */
export type CosmosMintGenesisState = {
    minter?: CosmosMintMinter | undefined;
    params?: CosmosMintParams | undefined;
};
