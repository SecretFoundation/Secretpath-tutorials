import type { Encoded, Opt } from '../../../api/types.js';
import type { JunoMintMinter, DecodedJunoMintMinter, JunoMintParams, DecodedJunoMintParams } from './mint.js';
export type EncodedJunoMintGenesisState = Encoded<'/juno.mint.GenesisState'>;
/**
 * Encodes a `GenesisState` protobuf message: defines the mint module's genesis state.
 * @param g_minter - `minter`: a space for holding current inflation information.
 * @param g_params - `params`: defines all the parameters of the module.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeJunoMintGenesisState: (atu8_minter?: Opt<Encoded<'/juno.mint.Minter'>>, atu8_params?: Opt<Encoded<'/juno.mint.Params'>>) => EncodedJunoMintGenesisState;
/**
 * A decoded protobuf GenesisState message
 *
 * Tuple where:
 *   - 0: minter - a space for holding current inflation information.
 *   - 1: params - defines all the parameters of the module.
 */
export type DecodedJunoMintGenesisState = [
    a_minter?: DecodedJunoMintMinter,
    a_params?: DecodedJunoMintParams
];
/**
 * Decodes a protobuf GenesisState message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedJunoMintGenesisState}
 */
export declare const decodeJunoMintGenesisState: (atu8_payload: Uint8Array) => DecodedJunoMintGenesisState;
/**
 * Destructures the fields of a {@link JunoMintGenesisState} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: minter - a space for holding current inflation information.
 *   - 1: params - defines all the parameters of the module.
 */
export declare const destructJunoMintGenesisState: (g_struct: JunoMintGenesisState) => [minter?: JunoMintMinter, params?: JunoMintParams];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_JUNO_MINT_GENESIS_STATE = "/juno.mint.GenesisState";
/**
 * JSON serialization of `juno.mint.GenesisState` - defines the mint module's genesis state.
 */
export type JunoMintGenesisState = {
    minter?: JunoMintMinter | undefined;
    params?: JunoMintParams | undefined;
};
