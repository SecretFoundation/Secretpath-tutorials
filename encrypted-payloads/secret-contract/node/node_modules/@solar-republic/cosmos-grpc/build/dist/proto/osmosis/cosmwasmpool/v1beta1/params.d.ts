import type { WeakUint64Str, CwUint64 } from '@solar-republic/types';
import type { Encoded, Opt } from '../../../../api/types.js';
export type EncodedOsmosisCosmwasmpoolParams = Encoded<'/osmosis.cosmwasmpool.v1beta1.Params'>;
/**
 * Encodes a `Params` protobuf message:
 * @param a_code_id_whitelists - `code_id_whitelist`: code_ide_whitelist contains the list of code ids that are allowed to be instantiated.
 * @param sg_pool_migration_limit - `pool_migration_limit`: the maximum number of pools that can be migrated at once via governance proposal. This is to have a constant bound on the number of pools that can be migrated at once and remove the possibility of an unlikely scenario of causing a chain halt due to a large migration.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeOsmosisCosmwasmpoolParams: (a_code_id_whitelists?: Opt<WeakUint64Str[]>, sg_pool_migration_limit?: Opt<WeakUint64Str>) => EncodedOsmosisCosmwasmpoolParams;
/**
 * A decoded protobuf Params message
 *
 * Tuple where:
 *   - 0: code_id_whitelist - code_ide_whitelist contains the list of code ids that are allowed to be instantiated.
 *   - 1: pool_migration_limit - the maximum number of pools that can be migrated at once via governance proposal. This is to have a constant bound on the number of pools that can be migrated at once and remove the possibility of an unlikely scenario of causing a chain halt due to a large migration.
 */
export type DecodedOsmosisCosmwasmpoolParams = [
    a_code_id_whitelists?: CwUint64[],
    sg_pool_migration_limit?: CwUint64
];
/**
 * Decodes a protobuf Params message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedOsmosisCosmwasmpoolParams}
 */
export declare const decodeOsmosisCosmwasmpoolParams: (atu8_payload: Uint8Array) => DecodedOsmosisCosmwasmpoolParams;
/**
 * Destructures the fields of a {@link OsmosisCosmwasmpoolParams} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: code_id_whitelist - code_ide_whitelist contains the list of code ids that are allowed to be instantiated.
 *   - 1: pool_migration_limit - the maximum number of pools that can be migrated at once via governance proposal. This is to have a constant bound on the number of pools that can be migrated at once and remove the possibility of an unlikely scenario of causing a chain halt due to a large migration.
 */
export declare const destructOsmosisCosmwasmpoolParams: (g_struct: OsmosisCosmwasmpoolParams) => [code_id_whitelist?: CwUint64[], pool_migration_limit?: CwUint64];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_OSMOSIS_COSMWASMPOOL_PARAMS = "/osmosis.cosmwasmpool.v1beta1.Params";
/**
 * JSON serialization of `osmosis.cosmwasmpool.v1beta1.Params` -
 */
export type OsmosisCosmwasmpoolParams = {
    code_id_whitelist?: CwUint64[] | undefined;
    pool_migration_limit?: CwUint64 | undefined;
};
