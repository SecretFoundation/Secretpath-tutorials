import { Protobuf, any } from 'src/protobuf-writer';
import { SR_LCD_FEEGRANT, lcd_query } from './_root.js';
export const queryFeegrantAllowance = lcd_query((sa_granter, sa_grantee) => [SR_LCD_FEEGRANT + 'allowance/' + sa_granter + '/' + sa_grantee], g => g.allowance);
export const queryFeegrantAllowances = lcd_query(sa_grantee => [SR_LCD_FEEGRANT + 'allowances/' + sa_grantee], g => g.allowances);
export const msgGrantAllowance = (sa_granter, sa_grantee, atu8_allowance) => {
    // construct body
    const kb_body = Protobuf()
        .v(10).s(sa_granter)
        .v(18).s(sa_grantee)
        .v(26).b(atu8_allowance);
    // construct as direct message
    return any('/cosmos.feegrant.v1beta1.MsgGrantAllowance', kb_body.o());
};
//# sourceMappingURL=feegrant.js.map