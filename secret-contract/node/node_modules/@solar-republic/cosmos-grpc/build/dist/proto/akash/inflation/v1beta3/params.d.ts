import type { Encoded, Opt } from '../../../../api/types.js';
export type EncodedAkashInflationParams = Encoded<'/akash.inflation.v1beta3.Params'>;
/**
 * Encodes a `Params` protobuf message: defines the parameters for the x/deployment package
 * @param s_inflation_decay_factor - `inflation_decay_factor`: InflationDecayFactor is the number of years it takes inflation to halve.
 * @param s_initial_inflation - `initial_inflation`: InitialInflation is the rate at which inflation starts at genesis. It is a decimal value in the range [0.0, 100.0].
 * @param s_variance - `variance`: Variance defines the fraction by which inflation can vary from ideal inflation in a block. It is a decimal value in the range [0.0, 1.0].
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeAkashInflationParams: (s_inflation_decay_factor?: Opt<string>, s_initial_inflation?: Opt<string>, s_variance?: Opt<string>) => EncodedAkashInflationParams;
/**
 * A decoded protobuf Params message
 *
 * Tuple where:
 *   - 0: inflation_decay_factor - InflationDecayFactor is the number of years it takes inflation to halve.
 *   - 1: initial_inflation - InitialInflation is the rate at which inflation starts at genesis. It is a decimal value in the range [0.0, 100.0].
 *   - 2: variance - Variance defines the fraction by which inflation can vary from ideal inflation in a block. It is a decimal value in the range [0.0, 1.0].
 */
export type DecodedAkashInflationParams = [
    s_inflation_decay_factor?: string,
    s_initial_inflation?: string,
    s_variance?: string
];
/**
 * Decodes a protobuf Params message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedAkashInflationParams}
 */
export declare const decodeAkashInflationParams: (atu8_payload: Uint8Array) => DecodedAkashInflationParams;
/**
 * Destructures the fields of a {@link AkashInflationParams} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: inflation_decay_factor - InflationDecayFactor is the number of years it takes inflation to halve.
 *   - 1: initial_inflation - InitialInflation is the rate at which inflation starts at genesis. It is a decimal value in the range [0.0, 100.0].
 *   - 2: variance - Variance defines the fraction by which inflation can vary from ideal inflation in a block. It is a decimal value in the range [0.0, 1.0].
 */
export declare const destructAkashInflationParams: (g_struct: AkashInflationParams) => [inflation_decay_factor?: string, initial_inflation?: string, variance?: string];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_AKASH_INFLATION_PARAMS = "/akash.inflation.v1beta3.Params";
/**
 * JSON serialization of `akash.inflation.v1beta3.Params` - defines the parameters for the x/deployment package
 */
export type AkashInflationParams = {
    inflation_decay_factor?: string | undefined;
    initial_inflation?: string | undefined;
    variance?: string | undefined;
};
