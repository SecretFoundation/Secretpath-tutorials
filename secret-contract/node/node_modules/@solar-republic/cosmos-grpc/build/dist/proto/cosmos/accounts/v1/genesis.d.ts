import type { WeakUint64Str, WeakAccountAddr, CwUint64, CwBase64, CwAccountAddr } from '@solar-republic/types';
import type { Encoded, Opt } from '../../../../api/types.js';
export type EncodedCosmosAccountsGenesisState = Encoded<'/cosmos.accounts.v1.GenesisState'>;
export type EncodedCosmosAccountsGenesisAccount = Encoded<'/cosmos.accounts.v1.GenesisAccount'>;
export type EncodedCosmosAccountsKVPair = Encoded<'/cosmos.accounts.v1.KVPair'>;
/**
 * Encodes a `GenesisState` protobuf message: defines the accounts' module's genesis state.
 * @param sg_account_number - `account_number`: the latest account number.
 * @param a_accounts - `accounts`: the genesis accounts.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeCosmosAccountsGenesisState: (sg_account_number?: Opt<WeakUint64Str>, a_accounts?: Opt<Encoded<'/cosmos.accounts.v1.GenesisAccount'>[]>) => EncodedCosmosAccountsGenesisState;
/**
 * Encodes a `GenesisAccount` protobuf message: defines an account to be initialized in the genesis state.
 * @param sa_address - `address`: the address of the account.
 * @param s_account_type - `account_type`: the account type of the account.
 * @param a_states - `state`: the account state represented as a slice of raw key value byte pairs.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeCosmosAccountsGenesisAccount: (sa_address?: Opt<WeakAccountAddr>, s_account_type?: Opt<string>, a_states?: Opt<Encoded<'/cosmos.accounts.v1.KVPair'>[]>) => EncodedCosmosAccountsGenesisAccount;
/**
 * Encodes a `KVPair` protobuf message: defines a key value pair.
 * @param atu8_key - `key`: the key of the pair.
 * @param atu8_value - `value`: the value of the pair.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeCosmosAccountsKVPair: (atu8_key?: Opt<Uint8Array>, atu8_value?: Opt<Uint8Array>) => EncodedCosmosAccountsKVPair;
/**
 * A decoded protobuf GenesisState message
 *
 * Tuple where:
 *   - 0: account_number - the latest account number.
 *   - 1: accounts - the genesis accounts.
 */
export type DecodedCosmosAccountsGenesisState = [
    sg_account_number?: CwUint64,
    a_accounts?: DecodedCosmosAccountsGenesisAccount[]
];
/**
 * Decodes a protobuf GenesisState message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedCosmosAccountsGenesisState}
 */
export declare const decodeCosmosAccountsGenesisState: (atu8_payload: Uint8Array) => DecodedCosmosAccountsGenesisState;
/**
 * A decoded protobuf GenesisAccount message
 *
 * Tuple where:
 *   - 0: address - the address of the account.
 *   - 1: account_type - the account type of the account.
 *   - 2: state - the account state represented as a slice of raw key value byte pairs.
 */
export type DecodedCosmosAccountsGenesisAccount = [
    sa_address?: CwAccountAddr,
    s_account_type?: string,
    a_state?: DecodedCosmosAccountsKVPair[]
];
/**
 * Decodes a protobuf GenesisAccount message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedCosmosAccountsGenesisAccount}
 */
export declare const decodeCosmosAccountsGenesisAccount: (atu8_payload: Uint8Array) => DecodedCosmosAccountsGenesisAccount;
/**
 * A decoded protobuf KVPair message
 *
 * Tuple where:
 *   - 0: key - the key of the pair.
 *   - 1: value - the value of the pair.
 */
export type DecodedCosmosAccountsKVPair = [
    atu8_key?: Uint8Array,
    atu8_value?: Uint8Array
];
/**
 * Decodes a protobuf KVPair message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedCosmosAccountsKVPair}
 */
export declare const decodeCosmosAccountsKVPair: (atu8_payload: Uint8Array) => DecodedCosmosAccountsKVPair;
/**
 * Destructures the fields of a {@link CosmosAccountsGenesisState} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: account_number - the latest account number.
 *   - 1: accounts - the genesis accounts.
 */
export declare const destructCosmosAccountsGenesisState: (g_struct: CosmosAccountsGenesisState) => [account_number?: CwUint64, accounts?: CosmosAccountsGenesisAccount[]];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_COSMOS_ACCOUNTS_GENESIS_STATE = "/cosmos.accounts.v1.GenesisState";
/**
 * JSON serialization of `cosmos.accounts.v1.GenesisState` - defines the accounts' module's genesis state.
 */
export type CosmosAccountsGenesisState = {
    account_number?: CwUint64 | undefined;
    accounts?: CosmosAccountsGenesisAccount[] | undefined;
};
/**
 * Destructures the fields of a {@link CosmosAccountsGenesisAccount} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: address - the address of the account.
 *   - 1: account_type - the account type of the account.
 *   - 2: state - the account state represented as a slice of raw key value byte pairs.
 */
export declare const destructCosmosAccountsGenesisAccount: (g_struct: CosmosAccountsGenesisAccount) => [address?: CwAccountAddr, account_type?: string, state?: CosmosAccountsKVPair[]];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_COSMOS_ACCOUNTS_GENESIS_ACCOUNT = "/cosmos.accounts.v1.GenesisAccount";
/**
 * JSON serialization of `cosmos.accounts.v1.GenesisAccount` - defines an account to be initialized in the genesis state.
 */
export type CosmosAccountsGenesisAccount = {
    address?: CwAccountAddr | undefined;
    account_type?: string | undefined;
    state?: CosmosAccountsKVPair[] | undefined;
};
/**
 * Destructures the fields of a {@link CosmosAccountsKVPair} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: key - the key of the pair.
 *   - 1: value - the value of the pair.
 */
export declare const destructCosmosAccountsKVPair: (g_struct: CosmosAccountsKVPair) => [key?: Uint8Array, value?: Uint8Array];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_COSMOS_ACCOUNTS_K_V_PAIR = "/cosmos.accounts.v1.KVPair";
/**
 * JSON serialization of `cosmos.accounts.v1.KVPair` - defines a key value pair.
 */
export type CosmosAccountsKVPair = {
    key?: CwBase64 | undefined;
    value?: CwBase64 | undefined;
};
