import type { WeakUint64Str, CwUint64 } from '@solar-republic/types';
import type { Encoded, Opt } from '../../../../../api/types.js';
export type EncodedCosmosGovModuleModule = Encoded<'/cosmos.gov.module.v1.Module'>;
/**
 * Encodes a `Module` protobuf message: the config object of the gov module.
 * @param sg_max_metadata_len - `max_metadata_len`: defines the maximum proposal metadata length. Defaults to 255 if not explicitly set.
 * @param s_authority - `authority`: defines the custom module authority. If not set, defaults to the governance module.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeCosmosGovModuleModule: (sg_max_metadata_len?: Opt<WeakUint64Str>, s_authority?: Opt<string>) => EncodedCosmosGovModuleModule;
/**
 * A decoded protobuf Module message
 *
 * Tuple where:
 *   - 0: max_metadata_len - defines the maximum proposal metadata length. Defaults to 255 if not explicitly set.
 *   - 1: authority - defines the custom module authority. If not set, defaults to the governance module.
 */
export type DecodedCosmosGovModuleModule = [
    sg_max_metadata_len?: CwUint64,
    s_authority?: string
];
/**
 * Decodes a protobuf Module message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedCosmosGovModuleModule}
 */
export declare const decodeCosmosGovModuleModule: (atu8_payload: Uint8Array) => DecodedCosmosGovModuleModule;
/**
 * Destructures the fields of a {@link CosmosGovModuleModule} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: max_metadata_len - defines the maximum proposal metadata length. Defaults to 255 if not explicitly set.
 *   - 1: authority - defines the custom module authority. If not set, defaults to the governance module.
 */
export declare const destructCosmosGovModuleModule: (g_struct: CosmosGovModuleModule) => [max_metadata_len?: CwUint64, authority?: string];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_COSMOS_GOV_MODULE_MODULE = "/cosmos.gov.module.v1.Module";
/**
 * JSON serialization of `cosmos.gov.module.v1.Module` - the config object of the gov module.
 */
export type CosmosGovModuleModule = {
    max_metadata_len?: CwUint64 | undefined;
    authority?: string | undefined;
};
