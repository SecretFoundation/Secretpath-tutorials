import type { WeakUint64Str, CwUint64 } from '@solar-republic/types';
import type { Encoded, Opt, WeakDurationStr } from '../../../../../api/types.js';
export type EncodedCosmosGroupModuleModule = Encoded<'/cosmos.group.module.v1.Module'>;
/**
 * Encodes a `Module` protobuf message: the config object of the group module.
 * @param xt_max_execution_period - `max_execution_period`: defines the max duration after a proposal's voting period ends that members can send a MsgExec to execute the proposal.
 * @param sg_max_metadata_len - `max_metadata_len`: defines the max length of the metadata bytes field for various entities within the group module. Defaults to 255 if not explicitly set.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeCosmosGroupModuleModule: (xt_max_execution_period?: Opt<number>, sg_max_metadata_len?: Opt<WeakUint64Str>) => EncodedCosmosGroupModuleModule;
/**
 * A decoded protobuf Module message
 *
 * Tuple where:
 *   - 0: max_execution_period - defines the max duration after a proposal's voting period ends that members can send a MsgExec to execute the proposal.
 *   - 1: max_metadata_len - defines the max length of the metadata bytes field for various entities within the group module. Defaults to 255 if not explicitly set.
 */
export type DecodedCosmosGroupModuleModule = [
    a_max_execution_period?: [
        string,
        number
    ],
    sg_max_metadata_len?: CwUint64
];
/**
 * Decodes a protobuf Module message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedCosmosGroupModuleModule}
 */
export declare const decodeCosmosGroupModuleModule: (atu8_payload: Uint8Array) => DecodedCosmosGroupModuleModule;
/**
 * Destructures the fields of a {@link CosmosGroupModuleModule} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: max_execution_period - defines the max duration after a proposal's voting period ends that members can send a MsgExec to execute the proposal.
 *   - 1: max_metadata_len - defines the max length of the metadata bytes field for various entities within the group module. Defaults to 255 if not explicitly set.
 */
export declare const destructCosmosGroupModuleModule: (g_struct: CosmosGroupModuleModule) => [max_execution_period?: number, max_metadata_len?: CwUint64];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_COSMOS_GROUP_MODULE_MODULE = "/cosmos.group.module.v1.Module";
/**
 * JSON serialization of `cosmos.group.module.v1.Module` - the config object of the group module.
 */
export type CosmosGroupModuleModule = {
    max_execution_period?: WeakDurationStr | undefined;
    max_metadata_len?: CwUint64 | undefined;
};
