import type { WeakInt64Str, CwInt64 } from '@solar-republic/types';
import type { Encoded, Opt } from '../../../../api/types.js';
import type { OsmosisMintMinter, DecodedOsmosisMintMinter, OsmosisMintParams, DecodedOsmosisMintParams } from './mint.js';
export type EncodedOsmosisMintGenesisState = Encoded<'/osmosis.mint.v1beta1.GenesisState'>;
/**
 * Encodes a `GenesisState` protobuf message: defines the mint module's genesis state.
 * @param g_minter - `minter`: an abstraction for holding current rewards information.
 * @param g_params - `params`: defines all the paramaters of the mint module.
 * @param sg_reduction_started_epoch - `reduction_started_epoch`: the first epoch in which the reduction of mint begins.
 * @returns a strongly subtyped Uint8Array protobuf message
 */
export declare const encodeOsmosisMintGenesisState: (atu8_minter?: Opt<Encoded<'/osmosis.mint.v1beta1.Minter'>>, atu8_params?: Opt<Encoded<'/osmosis.mint.v1beta1.Params'>>, sg_reduction_started_epoch?: Opt<WeakInt64Str>) => EncodedOsmosisMintGenesisState;
/**
 * A decoded protobuf GenesisState message
 *
 * Tuple where:
 *   - 0: minter - an abstraction for holding current rewards information.
 *   - 1: params - defines all the paramaters of the mint module.
 *   - 2: reduction_started_epoch - the first epoch in which the reduction of mint begins.
 */
export type DecodedOsmosisMintGenesisState = [
    a_minter?: DecodedOsmosisMintMinter,
    a_params?: DecodedOsmosisMintParams,
    sg_reduction_started_epoch?: CwInt64
];
/**
 * Decodes a protobuf GenesisState message
 * @param atu8_payload - raw bytes to decode
 * @returns a {@link DecodedOsmosisMintGenesisState}
 */
export declare const decodeOsmosisMintGenesisState: (atu8_payload: Uint8Array) => DecodedOsmosisMintGenesisState;
/**
 * Destructures the fields of a {@link OsmosisMintGenesisState} JSON message into a tuple of parsed ES equivalents
 * @param g_struct - the JSON message
 * @returns a tuple where:
 *   - 0: minter - an abstraction for holding current rewards information.
 *   - 1: params - defines all the paramaters of the mint module.
 *   - 2: reduction_started_epoch - the first epoch in which the reduction of mint begins.
 */
export declare const destructOsmosisMintGenesisState: (g_struct: OsmosisMintGenesisState) => [minter?: OsmosisMintMinter, params?: OsmosisMintParams, reduction_started_epoch?: CwInt64];
/**
 * Message type string constant
 */
export declare const SI_MESSAGE_TYPE_OSMOSIS_MINT_GENESIS_STATE = "/osmosis.mint.v1beta1.GenesisState";
/**
 * JSON serialization of `osmosis.mint.v1beta1.GenesisState` - defines the mint module's genesis state.
 */
export type OsmosisMintGenesisState = {
    minter?: OsmosisMintMinter | undefined;
    params?: OsmosisMintParams | undefined;
    reduction_started_epoch?: CwInt64 | undefined;
};
