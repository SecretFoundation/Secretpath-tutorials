import { createRIPEMD160 } from 'hash-wasm';
let y_ripemd160;
void createRIPEMD160().then((y_instance) => {
    y_ripemd160 = y_instance;
}, (e_load) => {
    console.error(`Failed to load ripemd160 WASM module:\n` + e_load.message);
});
export function ripemd160_ready() {
    return !!y_ripemd160;
}
export function ripemd160_sync(atu8_data) {
    if (!y_ripemd160)
        throw new Error('Attempted to use synchronous sha256 before the WASM module finished loading, or it failed to load');
    y_ripemd160.init();
    y_ripemd160.update(atu8_data);
    return y_ripemd160.digest('binary');
}
//# sourceMappingURL=ripemd160.js.map