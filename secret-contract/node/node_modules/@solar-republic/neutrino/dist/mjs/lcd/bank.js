import { F_IDENTITY } from '@blake.regalia/belt';
import { F_RPC_REQ_NO_ARGS, SR_LCD_BANK, lcd_query } from './_root.js';
export const queryBankBalances = lcd_query((sa_account, si_denom = '') => [
    SR_LCD_BANK + 'balances/' + sa_account + (si_denom ? '/by_denom' : ''),
    si_denom
        ? {
            denom: si_denom,
        } : {},
], g => g.balances ?? [g.balance]);
export const queryBankSpendableBalances = lcd_query(sa_account => [SR_LCD_BANK + 'spendable_balances/' + sa_account], g => g.balances);
export const queryBankParams = lcd_query(F_RPC_REQ_NO_ARGS, F_IDENTITY);
//# sourceMappingURL=bank.js.map